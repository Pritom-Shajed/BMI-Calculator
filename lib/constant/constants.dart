 import 'package:flutter/material.dart';

TextStyle boldText =  TextStyle(fontWeight: FontWeight.bold);
TextStyle textStyle = TextStyle(fontWeight: FontWeight.bold, fontSize: 48);

Widget horizontalSize(){
  return SizedBox(
    width: 20,
  );
}

Color selectedColor = Colors.pinkAccent;
Color unselectedColor = Colors.black54;

enum Gender{male,female}

